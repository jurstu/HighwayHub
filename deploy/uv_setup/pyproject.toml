[project]
name = "HighwayHub"
version = "0.0.1"
description = "Highway Hub - software for car data aquisition and display"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "requests>=2.32.5",
    "nicegui>=3.10.0,<3.15.0"
]

[dependency-groups]
dev = [

]